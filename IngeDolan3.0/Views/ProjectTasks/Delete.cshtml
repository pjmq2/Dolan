@model IngeDolan3._0.Models.ProjectTask

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Borrar</h2>
<h3>¿Realmente desea borrar la tarea seleccionada?</h3>
<div>
    <div class="navbar-right" style="display:inline-block;">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.StoryID)
            @Html.HiddenFor(model => model.ProjectID)

            <input type="submit" value="Borrar" class="btn btn-default" />
        }
    </div>

    <div class="navbar-right" style="display:inline-block;">
        
    </div>

    <h4>Tarea</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Descripcion
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descripcion)
        </dd>

        <dt>
            Tiempo Estimado
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EstimateTime)
        </dd>

        <dt>
            Prioridad
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Priority)
        </dd>

        <dt>
            Estado
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Estado)
        </dd>

        <dt>
            Modulo
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserStory.Modulo)
        </dd>

    </dl>

</div>

<div class="form-actions no-color">
    @Html.ActionLink("Regresar", "Details", new { projectId = Model.ProjectID, storyId = Model.StoryID, taskId = Model.TaskID }, new { @class = "btn btn-default" })
</div>
